server:
  port: 8081
  address: 0.0.0.0

spring:
  application:
    name: wepsiteturist

  datasource:
    url: jdbc:postgresql://localhost:5432/turistic_db
    username: postgres
    password: 1223
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  web:
    resources:
      static-locations:
        - file:uploads/

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 30MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: sunnatovasadbek004@gmail.com
    password: vwypxmcuqlpullfy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: "*"
            protocols: TLSv1.2

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.springframework.mail: DEBUG

jwt:
  secret: SUPER_SECRET_KEY_12345678901234567890
  access-expiration-ms: 900000
  refresh-expiration-ms: 604800000
